# Lung Segmentation — DeepLabV3+ (RTX 4060 Ti 8 GB)
# Install order matters: torch first, then the rest.
#
# Step 1 — PyTorch with CUDA 12.1 (matches most RTX 40-series driver stacks):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# Step 2 — everything else:
#   pip install -r requirements.txt

segmentation-models-pytorch>=0.3.3
opencv-python>=4.9.0
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.66.0
scikit-image>=0.22.0      # morphology + distance_transform_edt via scipy
scipy>=1.11.0
Pillow>=10.0.0
